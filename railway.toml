[build]
builder = "nixpacks"

[deploy]
startCommand = "rm -rf node_modules/.prisma && npx prisma generate && npx prisma db push && node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
